<UserControl x:Class="SupplementProcessor.UserControls.ZSumbol"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Height="140" Width="250">
    <Grid>
        <Border x:Name="BorderContour" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveCaptionBrushKey}}" BorderThickness="1" Visibility="Hidden"></Border >
        <Viewbox>
            <Path x:Name="PathZSumbol"
            Data="m 0,0 250,0 -250,140 250,0 h 0" Stroke="{DynamicResource {x:Static SystemColors.ActiveCaptionTextBrushKey}}" StrokeThickness="2" />
        </Viewbox>
    </Grid>
</UserControl>
